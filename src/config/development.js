
const host = "http://127.0.0.1"
const redirectUri = "http://127.0.0.1:8080";

const config = {
  apiUri: `${host}:8099/dash`,
  // apiUri: `${host}:8099`,
  redirectUri: redirectUri,
};
// http://10.244.186.111:8080
export default config;
