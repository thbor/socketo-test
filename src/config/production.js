
// const host = "http://10.244.168.180";
// const redirectUri = "http://10.244.168.180/dfm";

// const config = {
//   apiUri: `${host}/dfm`,
//   redirectUri: redirectUri,
//   authServer: `${host}/dfm`
// };

// export default config;

const host = "";

const config = {
  apiUri: `${host}/dash`,
  redirectUri: `${host}/dash`,
};

export default config;
